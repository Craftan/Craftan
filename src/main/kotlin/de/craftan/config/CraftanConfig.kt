package de.craftan.config

data class CraftanConfig(
    val singleLobby: Boolean,
// description = "Whether craftan should handle this server as a single lobby, or allow multiple lobbies per this server."
)
