package de.craftan.io

/**
 * Models an util interface, to interact with files outside of craftan
 */
interface FileAdapter {
    /**
     * Loads the given file with the given defaults
     */
    fun load()
}
